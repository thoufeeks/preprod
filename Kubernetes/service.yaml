apiVersion: v1
kind: Service
metadata:
  name: netflix-app
  labels:
    app: netflix-app
  namespace: preprod
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: netflix-app
